# Runtime Throws Adapter Test Log

- Read-only audit confirmed the nine runtime throws/raises annotations are public synchronous externals in pinned Stdlib_JSON.res; its public interface omits those attributes. Exact public export shapes must be combined with verified implementation contracts, not inferred from OrThrow naming or shared JavaScript primitive spelling.
- Parent selected explicit adapter-wide activation: `throwsRuntime: "rescript-12.3.1"` / `--throws-runtime rescript-12.3.1` checks every requested source even without local annotations. Default source-local and configured-project behavior stays unchanged; disabling no-unhandled-throws bypasses adapter analysis.
- Added public Linter coverage for all nine JSON contracts, canonical runtime paths, lexical aliases/opens/includes/shadows, catch-all versus named handlers, distinct call-site diagnostics, strict unsupported boundaries, hidden public exports and project interface precedence.
- Added deterministic CLI checks for opt-in defaults, single findings, JSON configuration, handled calls, unavailable metadata errors, disabled-rule bypass and invalid adapter versions. Parent owns test registration and serialized verification; no builds run here.
- Final serialized `make check coverage` passed all 49 public integration cases, six CLI checks, and 76 upstream parity checks. Overall coverage is 95.40% (6596/6914); Throws_runtime is 96.15%, Throws_project 100%, and No_unhandled_throws 98.87%.
- Read-only final review confirmed disabled-rule bypass, explicit source/project activation, project-root shadowing before fixed-point indexing, unchanged project interface precedence, and fail-explicit opaque runtime scopes. No additional concrete defect was identified. Production and tests are frozen; parent owns release/packaging verification.
