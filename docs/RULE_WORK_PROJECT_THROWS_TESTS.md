# Project Throws Integration Test Log

- Read the public Linter/Rule_config boundary, project discovery fixtures, and existing local throws contract tests; applied functional-code-style and writing-functions.
- Added isolated temporary-project tests through `Linter.lint_source_with_rules`, enabling only `no-unhandled-throws` so unrelated policies cannot obscure contract behavior.
- Findings must identify the precise callee source span, current source filename, exact rule ID, and absence of fixes. Unsupported inputs must return typed `Analysis_errors` with the intended analysis rule, not ordinary findings or arbitrary errors.
- Cases cover imported contracts without local annotations, named/catchall/switch handling, lexical aliases/opens/includes/shadowing, transitive dependencies, interface precedence/private values, nested modules, and per-file exception identity.
- Boundary cases cover disabled-rule metadata bypass, missing project sources, unsupported imported annotations, unchanged no-annotation behavior, and unsaved implementation/interface overlays.
- Parent owns production changes and serialized builds. New cases are initially unverified and are not assertions that the prior implementation supported project contracts.
- Aligned dependency-scoped activation with the intended boundary: an unrelated annotated provider does not activate checks for an unknown caller dependency; combining a recognized annotated dependency with an unresolved call does fail analysis.
- Corrected the missing-directory expectation to the existing typed `Read_error` contract rather than inventing a new project-analysis category.
- Added identity stress cases for implementation/interface agreement, implementation exception aliases exposed as separate interface declarations, older hidden exceptions versus exported shadow declarations, and local exceptions distinct from provider exports.
- Provider indexing tests ensure opaque calls, await, and callback escapes inside bodies are not analyzed while importing contracts. Malformed provider metadata asserts the exact provider filename and payload range, not a relocated caller error.
- Extended interface coverage for known `open`, `include module type of`, and module aliases using forms confirmed in vendored parser fixtures. Negative cases ensure neither opened exceptions nor enclosing interface exceptions leak into nested public export namespaces.
- Parent confirmed all 79 first-wave integration cases passed. Follow-up adds nine fixed-point convergence, cyclic dependency, metadata-placement, and dependency-isolation cases.
- Added `test/project_throws_cli.sh`, registered as a Cram dependency with stable expected output. It checks CLI/config activation, source-only compatibility, named handlers, disabled analysis, provider error locations/status, fail-closed fix mode, and interface precedence.
- Read-only production review identified a false-clean risk in declaration indexing: an unresolved qualified value alias could become `Plain` when provider bodies are intentionally skipped. Added a focused unsupported-metadata regression and reported the production fix to the parent.
- Parent's full checks and coverage passed after the qualified-alias correction (Throws_project 100%, No_unhandled_throws 99.42%). A related read-only review found exported aggregate/higher-order initializers could erase annotated function identities.
- Added seven initializer regressions: local tuples, imported destructuring, higher-order arguments and records must fail explicitly; direct aliases keep their contracts, initializer call effects do not become caller findings, and nested callback bodies stay outside metadata indexing. These await the parent's grouped production correction and serialized verification.
- Final serialized `make check coverage` passed after both declaration-indexing corrections: all 96 project integration cases and all seven CLI checks passed. Overall coverage is 95.40%; Throws_project is 100% and No_unhandled_throws is 98.87%. Production and tests are frozen; no pending failures remain from this test batch.
